export const quizData = [
  { question: "What is the amortized time complexity of appending an element to the end of a Dynamic Array (ArrayList)?", options: ["$O(1)$", "$O(\\log N)$", "$O(N)$", "$O(N^2)$"], correctIndex: 0, explanation: "While resizing takes $O(N)$, it happens infrequently enough that the average cost per insertion over time is constant $O(1)$." },
  { question: "Why is iterating over a 2D array in Row-Major order faster than Column-Major order in Java?", options: ["It uses less memory", "CPU Cache Locality (prefetching contiguous blocks)", "It avoids integer overflow", "The JVM optimizes the loop"], correctIndex: 1, explanation: "Hardware prefetchers load contiguous memory into the CPU cache. Row-major iteration aligns with this, minimizing expensive main-memory cache misses." },
  { question: "What is the time complexity to delete an element from the middle of an ArrayList?", options: ["$O(1)$", "$O(\\log N)$", "$O(N)$", "$O(N \\log N)$"], correctIndex: 2, explanation: "All elements after the deleted index must be shifted one position to the left to fill the gap, requiring $O(N)$ time." },
  { question: "If an ArrayList of capacity 10 is full, and it uses a 2x growth factor, what happens on the next `add()`?", options: ["It throws an IndexOutOfBoundsException", "It allocates a new array of size 20 and copies elements", "It drops the oldest element", "It converts to a LinkedList"], correctIndex: 1, explanation: "Dynamic arrays handle overflow by allocating a larger backing array (usually 1.5x or 2x) and copying the existing elements." },
  { question: "Which data structure has better spatial locality?", options: ["Singly Linked List", "Doubly Linked List", "Array", "Binary Search Tree"], correctIndex: 2, explanation: "Arrays are allocated in a single contiguous block of memory, providing perfect spatial locality for CPU caching." }
];