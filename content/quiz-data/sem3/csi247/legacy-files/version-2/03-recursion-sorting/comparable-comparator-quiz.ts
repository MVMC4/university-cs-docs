export const quizData = [{"question":"What method must a class implement to define its natural sorting order?","options":["`compare(T o1, T o2)`","`compareTo(T o)`","`equals(Object o)`","`sort(T[] a)`"],"correctIndex":1,"explanation":"`Comparable<T>` requires the `compareTo(T o)` method to define natural ordering."},{"question":"What does it mean if `a.compareTo(b)` returns a negative integer?","options":["`a` is greater than `b`.","`a` is equal to `b`.","`a` is less than `b`.","`a` and `b` cannot be compared."],"correctIndex":2,"explanation":"A negative return value indicates that `a` is strictly less than `b` in the natural ordering."},{"question":"What is the primary advantage of using `Comparator<T>` over `Comparable<T>`?","options":["It is faster.","It allows defining custom, external sorting orders without modifying the original class.","It supports primitive types.","It does not require generics."],"correctIndex":1,"explanation":"`Comparator` allows you to sort objects in multiple different ways (e.g., by name, then by age) without changing the class's source code."},{"question":"In the method signature `Arrays.sort(T[] a, Comparator<? super T> c)`, why is `? super T` used?","options":["Because the array is a Consumer of the Comparator.","Because the Comparator is a Consumer of the array elements, so it must be able to accept `T` or any of its supertypes.","Because it prevents null pointers.","It is a syntax error."],"correctIndex":1,"explanation":"PECS: The Comparator consumes (compares) elements of type `T`. Therefore, it can be a `Comparator<T>` or a `Comparator<SupertypeOfT>`."},{"question":"What exception is thrown if a `Comparator` violates the contract of transitivity?","options":["`NullPointerException`","`IllegalArgumentException`","`ConcurrentModificationException`","No exception is thrown at compile time, but sorting algorithms may behave unpredictably or infinite loop."],"correctIndex":3,"explanation":"The compiler cannot verify the mathematical contract of a `Comparator`. Violating it leads to undefined behavior at runtime, such as incorrect sorting or infinite loops in some algorithms."},{"question":"What does it mean for `compareTo` to be 'consistent with equals'?","options":["`compareTo` must return 0 if and only if `equals` returns true.","`compareTo` must return the same integer as `hashCode`.","`compareTo` must be faster than `equals`.","They are exactly the same method."],"correctIndex":0,"explanation":"It is strongly recommended that `a.compareTo(b) == 0` yields the same boolean result as `a.equals(b)`. Sorted sets like `TreeSet` use `compareTo` for equality checks."},{"question":"How does a Binary Search Tree (BST) use `Comparable` or `Comparator`?","options":["To hash the elements.","To decide whether to traverse left or right during insertion and search.","To balance the tree.","To iterate through the tree."],"correctIndex":1,"explanation":"The BST invariant relies on ordering. It uses `compareTo` or `compare` to determine if a value is smaller (go left) or larger (go right)."},{"question":"Which interface should you implement if you are writing a `Student` class and want it to be sortable by `Collections.sort()` by default?","options":["`Comparator<Student>`","`Comparable<Student>`","`Iterable<Student>`","`Serializable`"],"correctIndex":1,"explanation":"To have a default natural ordering, the class itself must implement `Comparable<T>`."},{"question":"What is the return type of `compare(T o1, T o2)`?","options":["`boolean`","`int`","`double`","`void`"],"correctIndex":1,"explanation":"Both `compareTo` and `compare` return an `int` representing the relative order of the two objects."},{"question":"If you sort a list using a `Comparator`, does the original class need to implement `Comparable`?","options":["Yes, always.","No, `Comparator` provides an external ordering mechanism.","Only if the list contains Strings.","Yes, but only for primitive types."],"correctIndex":1,"explanation":"`Comparator` is specifically designed to sort objects that do not implement `Comparable`, or to override their natural ordering."},{"question":"What happens if `a.compareTo(b)` throws a `NullPointerException`?","options":["The sorting algorithm catches it and skips the element.","The sorting algorithm terminates abruptly.","`a` is considered greater than `b`.","`a` is considered equal to `b`."],"correctIndex":1,"explanation":"`compareTo` should handle nulls gracefully (or throw NPE if nulls are not permitted), but if it throws, the sorting method will propagate the exception and fail."},{"question":"In Java 8+, how can you easily create a `Comparator` that sorts `String` by length?","options":["`new Comparator<String>(String::length)`","`Comparator.comparingInt(String::length)`","`Arrays.sort(String::length)`","`String.compareByLength()`"],"correctIndex":1,"explanation":"Java 8 introduced static factory methods in the `Comparator` interface, such as `comparingInt`, to easily create comparators using method references."},{"question":"Why is it dangerous to use a `Comparator` that is not consistent with `equals` in a `TreeSet`?","options":["It causes a compilation error.","The `TreeSet` might consider two unequal objects to be equal (if `compare` returns 0) and refuse to add the second one.","It makes the `TreeSet` unbalanced.","It converts the `TreeSet` into a `HashSet`."],"correctIndex":1,"explanation":"`TreeSet` uses the `Comparator` for all equality checks. If `compare(a, b) == 0` but `!a.equals(b)`, the `TreeSet` will treat them as duplicates and discard one."},{"question":"What is the time complexity of `compareTo` for `String` in the worst case?","options":["$O(1)$","$O(\\log n)$","$O(n)$ where $n$ is the length of the string","$O(n^2)$"],"correctIndex":2,"explanation":"String comparison may need to check every character up to the length of the shorter string, resulting in $O(n)$ time."},{"question":"Which method is used to sort an array of primitive `int` in Java?","options":["`Arrays.sort(int[] a, Comparator c)`","`Arrays.sort(int[] a)`","`Collections.sort(int[] a)`","`int[] sorted = a.sort()`"],"correctIndex":1,"explanation":"Primitive arrays do not use `Comparator` because they don't have objects to compare. `Arrays.sort(int[] a)` uses a dual-pivot Quicksort."}];