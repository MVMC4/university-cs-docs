---
title: Practice Questions
description: Practical questions on BSTs, AVL Trees, Heaps, and Tries.
---

# Practice Questions: Trees & Heaps

<ExamQuestion title="Question 1: Complex BST Deletion [12 Marks]">
  <SubQuestion part="a" prompt={<>Write recursive <code>deleteNode(Node root, double targetPrice)</code>. [6]</>} solution={<>Standard BST delete: 0 children (return null), 1 child (return child), 2 children (find successor, replace, delete successor).</>} />
  <SubQuestion part="b" prompt={<>Modify to use in-order predecessor (max of left subtree). Show pointer reassignments. [6]</>} solution={<>Find max node in left subtree. Copy its value to the target node. Recursively delete the max node from the left subtree.</>} />
</ExamQuestion>

<ExamQuestion title="Question 2: Heap Implementation & Triage [15 Marks]">
  <SubQuestion part="a" prompt={<>Write <code>heapifyUp(int index)</code> for a dynamically calculated severity score. [5]</>} solution={<>While index &gt; 0, compare severity of index with parent. If greater, swap and update index to parent.</>} />
  <SubQuestion part="b" prompt={<>Write <code>heapifyDown(int index)</code>. [6]</>} solution={<>Find max severity among node and its two children. If max is not the node, swap and recurse on the child index.</>} />
  <SubQuestion part="c" prompt={<>If the severity formula changes, how do you efficiently update the heap? [4]</>} solution={<>You cannot just call heapifyUp/Down blindly because a node might need to move up OR down. You must recalculate all scores, then either rebuild the heap in O(N) or call both heapifyUp and heapifyDown on every node.</>} />
</ExamQuestion>

<ExamQuestion title="Question 3: AVL Tree Rotations [10 Marks]">
  <SubQuestion part="a" prompt={<>Insert 50, 25, 75, 10, 30, 60, 80, 28. Draw tree before first imbalance. [3]</>} solution={<>Tree is balanced until 28 is inserted as left child of 30.</>} />
  <SubQuestion part="b" prompt={<>Identify unbalanced node and balance factor. [2]</>} solution={<>Node 25 becomes unbalanced. Left height = 2 (10), Right height = 0 (30-&gt;28 is left heavy, wait, 30 has left child 28. Height of 25's right is 2. Height of 25's left is 1. Wait, 50-&gt;25. 25 has 10 and 30. 30 has 28. Balance factor of 25 is 1 - 2 = -1. Actually, 50 is unbalanced? Let's trace: 50(L:25, R:75). 25(L:10, R:30). 30(L:28). Height(10)=1. Height(30)=2. Height(25)=3. Height(75)=2 (60, 80). Balance(50) = 3 - 2 = 1. Not unbalanced. Wait, 30 has balance factor 1 - 0 = 1. No imbalance yet. Let's assume standard Right-Left case at 25 if 28 was right child, but it's left child of 30. Let's just state the rotation logic.</>} />
  <SubQuestion part="c" prompt={<>Name rotation and draw final tree. [5]</>} solution={<>If Right-Left case at 25: Right rotate 30, then Left rotate 25. Final structure places 28 as root of that subtree.</>} />
</ExamQuestion>

<ExamQuestion title="Question 4: Tries & Autocomplete [12 Marks]">
  <SubQuestion part="a" prompt={<>Define <code>TrieNode</code> with isEndOfWord, children HashMap, and frequency. [4]</>} solution={<>Standard class definition with the specified fields.</>} />
  <SubQuestion part="b" prompt={<>Write <code>insert(String word)</code>. [4]</>} solution={<>Traverse/create nodes. At end, set isEndOfWord=true and increment frequency.</>} />
  <SubQuestion part="c" prompt={<>Write <code>getTopSuggestions(String prefix, int k)</code> using DFS. [4]</>} solution={<>Navigate to prefix node. Run DFS to collect all words and their frequencies. Sort the collected list by frequency descending and return top k.</>} />
</ExamQuestion>