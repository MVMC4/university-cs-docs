---
title: Chapter Review
description: Self-assessment checklist, flashcards, and quizzes for Linear Data Structures.
---

# Chapter Review

import { flashcardData } from '@/content/flashcard-data/sem3/csi247/04-linear-structures/flashcards';
import { quizData as linkedListsQuiz } from '@/content/quiz-data/sem3/csi247/04-linear-structures/linked-lists-quiz';
import { quizData as stacksQueuesQuiz } from '@/content/quiz-data/sem3/csi247/04-linear-structures/stacks-queues-quiz';
import { quizData as stacksAppsQuiz } from '@/content/quiz-data/sem3/csi247/04-linear-structures/stacks-applications-quiz';

## Self-Assessment Checklist

<Checklist
  title="Chapter 4  --  You Should Be Able To..."
  rules={[
    { type: "must", text: "Implement singly linked list insert (head, tail, middle) and delete by drawing pointer diagrams before writing code." },
    { type: "must", text: "Implement a circular queue using modulo arithmetic: rear = (rear + 1) % capacity." },
    { type: "must", text: "Trace the Shunting Yard algorithm by hand for a multi-operator expression including parentheses." },
    { type: "must", text: "Evaluate a postfix expression using a stack: push operands, pop two on operator, push result." },
    { type: "must", text: "Explain why java.util.Stack is legacy and why Deque<> (ArrayDeque) is the idiomatic replacement." },
    { type: "must", text: "Implement Floyd's cycle detection (slow/fast pointer) on a linked list." },
    { type: "avoid", text: "Forgetting that a circular queue of size N can only hold N-1 elements if front==rear signals empty  --  use a size counter." },
    { type: "avoid", text: "Updating next pointers without saving the reference to the next node first during linked list manipulation." },
    { type: "note", text: "A Monotonic Stack keeps elements in strictly increasing or decreasing order  --  useful for next-greater-element problems." },
  ]}
/>

---

## Flashcards

<FlashcardDeck cards={flashcardData} title="Linear Structures Deck" />

---

## Quick Quiz

<Quiz
  title="Chapter 4 Quick Check"
  questions={[
    {
      question: "When evaluating a postfix expression, what happens when you encounter an operator?",
      options: ["Push it to the stack", "Pop one operand and apply", "Pop two operands, apply, and push result", "Clear the stack"],
      correctIndex: 2,
      explanation: "Postfix evaluation requires two operands for any binary operator. You pop the top two, compute, and push the result back."
    }
  ]}
/>

---

## Full Chapter Assessments

<Quiz title="Linked Lists Assessment" questions={linkedListsQuiz} />
<Quiz title="Stacks & Queues Assessment" questions={stacksQueuesQuiz} />
<Quiz title="Stack Applications Assessment" questions={stacksAppsQuiz} />
