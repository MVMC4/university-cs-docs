---
title: Chapter Review
description: Self-assessment checklist, flashcards, and quizzes for Hash Tables.
---

# Chapter Review

import { flashcardData } from '@/content/flashcard-data/sem3/csi247/07-hash-tables/flashcards';
import { quizData as hashTablesQuiz } from '@/content/quiz-data/sem3/csi247/07-hash-tables/hash-tables-quiz';
import { quizData as collisionQuiz } from '@/content/quiz-data/sem3/csi247/07-hash-tables/collision-resolution-quiz';

## Self-Assessment Checklist

<Checklist
  title="Chapter 7  --  You Should Be Able To..."
  rules={[
    { type: "must", text: "Trace separate chaining collision resolution for a sequence of insertions on a table of size n." },
    { type: "must", text: "Trace linear probing and quadratic probing for the same collision scenario and explain primary clustering." },
    { type: "must", text: "Calculate the load factor alpha = n/m and explain why Java's HashMap rehashes at alpha > 0.75." },
    { type: "must", text: "Explain why you must override both equals() and hashCode() together  --  and what breaks if you only override one." },
    { type: "must", text: "State average and worst-case complexity for HashMap get/put under separate chaining." },
    { type: "avoid", text: "Using mutable objects as HashMap keys  --  if the key's hashCode changes after insertion, it becomes permanently unreachable." },
    { type: "avoid", text: "Confusing HashMap (unordered) with TreeMap (sorted by key) and LinkedHashMap (insertion-ordered)." },
    { type: "note", text: "Double hashing h(k,i) = h1(k) + i*h2(k) eliminates both primary and secondary clustering." },
  ]}
/>

---

## Flashcards

<FlashcardDeck cards={flashcardData} title="Hash Tables Deck" />

---

## Quick Quiz

<Quiz
  title="Chapter 7 Quick Check"
  questions={[
    {
      question: "A developer overrides equals() in a Student class but NOT hashCode(). What breaks?",
      options: [
        "equals() will always return false",
        "HashMap.get() will fail to find keys that are .equals() but not the same object",
        "The compiler throws an error",
        "Sorting with TreeMap breaks"
      ],
      correctIndex: 1,
      explanation: "HashMap first compares hashCodes to find the bucket. Two equal Students will land in different buckets if hashCode() is not overridden, making get() return null even though the key 'exists'."
    }
  ]}
/>

---

## Full Chapter Assessments

<Quiz title="Hash Tables Assessment" questions={hashTablesQuiz} />
<Quiz title="Collision Resolution Deep Dive" questions={collisionQuiz} />
