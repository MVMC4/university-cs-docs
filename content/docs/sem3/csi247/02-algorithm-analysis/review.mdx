---
title: Chapter Review
description: Self-assessment checklist, flashcards, and quizzes for Algorithm Analysis.
---

# Chapter Review

import { flashcardData } from '@/content/flashcard-data/sem3/csi247/02-algorithm-analysis/flashcards';
import { quizData as bigOQuiz } from '@/content/quiz-data/sem3/csi247/02-algorithm-analysis/big-o-quiz';

## Self-Assessment Checklist

<Checklist
  title="Chapter 2  --  You Should Be Able To..."
  rules={[
    { type: "must", text: "State the formal Big-O definition and identify the dominant term in any polynomial expression." },
    { type: "must", text: "Rank the standard complexity classes: O(1) < O(log n) < O(n) < O(n log n) < O(n^2) < O(2^n) < O(n!)." },
    { type: "must", text: "Apply all three cases of the Master Theorem to a given recurrence relation." },
    { type: "must", text: "Explain the difference between Big-O (upper bound), Big-Omega (lower bound), and Big-Theta (tight bound)." },
    { type: "must", text: "Calculate the amortized cost of ArrayList.add() using the aggregate method." },
    { type: "must", text: "Analyse a nested loop and derive its time complexity by counting primitive operations." },
    { type: "avoid", text: "Confusing worst-case with Big-O  --  Big-O is a notation for any bound, not just worst-case." },
    { type: "note", text: "Little-o (o) means strictly slower growth; it excludes the tight bound case." },
  ]}
/>

---

## Flashcards

<FlashcardDeck cards={flashcardData} title="Algorithm Analysis Deck" />

---

## Quick Quiz

<Quiz
  title="Chapter 2 Quick Check"
  questions={[
    {
      question: "Which case of the Master Theorem applies to $T(n) = 2T(n/2) + \\mathcal{O}(n)$?",
      options: ["Case 1", "Case 2", "Case 3", "Master Theorem does not apply"],
      correctIndex: 1,
      explanation: "$n^(\\log_2 2) = n^1$. $f(n) = \\mathcal{O}(n)$. Since they are equal, it's Case 2, resulting in $\\mathcal{O}(n \\log n)$."
    }
  ]}
/>

---

## Full Chapter Assessment

<Quiz title="Algorithm Analysis Assessment" questions={bigOQuiz} />
