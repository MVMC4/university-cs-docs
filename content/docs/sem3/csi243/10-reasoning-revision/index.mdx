---
title: "Chapter Overview"
description: "Chapter 10 learning path for specification and correctness, equational reasoning, error classification and testing, and the cost of a definition."
---

## Chapter 10: Reasoning, Testing, and Revision

The previous nine chapters taught you to write Haskell. This one is about **justifying** what you wrote: saying what a function should do, arguing that it does, classifying what goes wrong, and knowing what it costs.

These are the skills the written examination tests, and the ones that separate code that happens to work from code you can defend.

| Order | Lesson | Main question |
|---:|---|---|
| 1 | [Specification and Correctness](/docs/sem3/csi243/10-reasoning-revision/notes/specification) | What does a function promise, and what does "correct" actually claim? |
| 2 | [Equational Reasoning](/docs/sem3/csi243/10-reasoning-revision/notes/equational-reasoning) | How do I argue about all inputs rather than a tested few? |
| 3 | [Errors and Testing](/docs/sem3/csi243/10-reasoning-revision/notes/errors-and-testing) | How do I classify a failure and choose test cases that find faults? |
| 4 | [The Cost of a Definition](/docs/sem3/csi243/10-reasoning-revision/notes/complexity) | Why are two definitions of the same function so different in speed? |

After the notes, use [Practice Questions](/docs/sem3/csi243/10-reasoning-revision/questions) and then [Review](/docs/sem3/csi243/10-reasoning-revision/review).

## The three levels of evidence

Each is stronger than the last, and each has a place:

| Evidence | Shows | Limitation |
|---|---|---|
| It compiles | Types are used consistently | Says nothing about the values computed |
| Tests pass | It worked for those inputs | Says nothing about untested inputs |
| A completed induction | It holds for every finite input | Assumes termination, which is argued separately |

A compiler proves type consistency, not that your formula matches the problem. That is why every laboratory has asked for a specification, a dry run, **and** tests.

## Revision route

Working back through the course, the questions most likely to be asked:

| Chapter | Be able to |
|---|---|
| 1–2 | Explain purity, immutability, and effects; predict the type and value of an expression |
| 3 | Read a signature; separate a pure calculation from its IO boundary |
| 4 | Implement one specification with `if`, patterns, guards, and `case`, and justify the choice |
| 5 | Identify base case and recursive step; dry run; explain non-termination |
| 6 | Recurse over `[]` and `(x:xs)`; choose between a list and a tuple; read a comprehension |
| 7 | Explain currying; use `map`, `filter`, and folds; predict a fold by substitution |
| 8 | Structure a program in modules; declare and use custom types |
| 9 | Read constraints; diagnose a "No instance for" error; use `Maybe` |
| 10 | Write a specification; prove a property by induction; classify an error; compare costs |

## Chapter outcomes

- Write a specification with a purpose, precondition, and postcondition.
- Partition an input set into valid and invalid subsets, and relate that to a type.
- Explain that correctness is relative to a specification, and that type checking is not correctness.
- Use referential transparency to substitute equals for equals.
- Prove a property of a list function by structural induction.
- Distinguish syntax from semantic errors, and compile-time from run-time failures.
- Choose normal, boundary, and invalid test cases, and test just outside range boundaries.
- Count reduction steps and distinguish linear, quadratic, and exponential growth.
- Explain laziness with thunks, and why an infinite list can be usable.
