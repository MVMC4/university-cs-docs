{
  "content/docs/sem3/csi247/01-java-foundations/basic.mdx": "---\ntitle: Java Basics, Arrays & Memory\ndescription: Core Java syntax, control flow, 1D/2D arrays (matrices), and JVM memory management.\n---\n\n# Java Basics, Arrays & Memory\n\nThis document serves as a complete revision guide for Core Java programming fundamentals, control flow, matrix operations, and JVM memory architecture.\n\n> **Quick Tip**: Java is case-sensitive. `myVariable` and `myvariable` are different!\n\n---\n\n## 1. Data Types & Variables\n\nJava is a **strongly typed** language, meaning every variable must be declared with a specific data type. This catches errors at compile-time rather than runtime.\n\n### Primitive Data Types\nThese are the most basic data types built into Java. They hold simple values and are stored in the **Stack** memory for fast access.\n\n<SafeImage\n  src=\"/images/sem3/csi247/primitive-types-chart.png\"\n  alt=\"Java Primitive Data Types\"\n  caption=\"Figure 1: The 8 primitive types with their sizes and ranges\"\n  className=\"w-full max-w-4xl mx-auto\"\n/>\n\n| Type | Size | Range | Default | Example |\n| :--- | :--- | :--- | :--- | :--- |\n| `byte` | 1 byte | -128 to 127 | 0 | `byte b = 100;` |\n| `short` | 2 bytes | -32,768 to 32,767 | 0 | `short s = 30000;` |\n| `int` | 4 bytes | ±2 billion | 0 | `int i = 100000;` |\n| `long` | 8 bytes | ±9 quintillion | 0L | `long l = 100000L;` |\n| `float` | 4 bytes | 6-7 decimal digits | 0.0f | `float f = 5.75f;` |\n| `double`| 8 bytes | 15 decimal digits | 0.0d | `double d = 19.99;` |\n| `boolean`| 1 bit | true/false | false | `boolean b = true;` |\n| `char` | 2 bytes | Unicode character | '\\u0000' | `char c = 'A';` |\n\n### Reference Data Types\nThese refer to objects and arrays. They are stored in the **Heap** memory, and the variable holds a *reference* (memory address) to the actual data.\n\n```java\n// Reference types examples\nString name = \"Alice\";           // String object\nint[] numbers = {1, 2, 3};       // Array object\nScanner sc = new Scanner(System.in); // Scanner object\n```\n\n> **Important**: Strings are **NOT** primitives! They're immutable objects.\n\n### Type Casting\nConverting one data type to another.\n\n**Widening (Implicit)** - Automatic, safe:\n```java\nint myInt = 9;\ndouble myDouble = myInt; // 9.0 - no data loss\n```\n\n**Narrowing (Explicit)** - Manual, may lose data:\n```java\ndouble myDouble = 9.78;\nint myInt = (int) myDouble; // 9 - decimal truncated!\n```\n\n---\n\n## 2. Control Flow Statements\n\nControl flow statements dictate the order in which your code is executed.\n\n### Conditional Statements\n\n#### `if`, `else if`, `else`\nUsed to execute blocks of code based on boolean conditions.\n\n```java\nint score = 85;\n\nif (score >= 90) {\n    System.out.println(\"Grade: A\");\n} else if (score >= 80) {\n    System.out.println(\"Grade: B\"); // Executes\n} else if (score >= 70) {\n    System.out.println(\"Grade: C\");\n} else {\n    System.out.println(\"Grade: F\");\n}\n```\n\n#### `switch` Statement\nAn alternative to long `if-else if` chains.\n\n```java\nString day = \"WEDNESDAY\";\n\nswitch (day) {\n    case \"MONDAY\":\n        System.out.println(\"Start of work week\");\n        break;\n    case \"WEDNESDAY\":\n        System.out.println(\"Midweek!\"); // Executes\n        break;\n    case \"FRIDAY\":\n        System.out.println(\"Weekend soon\");\n        break;\n    default:\n        System.out.println(\"Another day\");\n}\n```\n\n> **Note**: Without `break`, execution \"falls through\" to the next case!\n\n### Loops\n\n1. **`for` Loop** - Known iterations:\n```java\nfor (int i = 0; i < 5; i++) {\n    System.out.println(\"Iteration: \" + i);\n}\n```\n\n2. **`while` Loop** - Condition-based:\n```java\nint count = 0;\nwhile (count < 3) {\n    System.out.println(\"Count: \" + count);\n    count++;\n}\n```\n\n3. **Enhanced `for-each` Loop** - Arrays/Collections:\n```java\nString[] fruits = {\"Apple\", \"Banana\", \"Cherry\"};\nfor (String fruit : fruits) {\n    System.out.println(fruit);\n}\n```\n\n---\n\n## 3. Arrays & Matrices\n\nAn array is a fixed-size, contiguous block of memory storing multiple values of the **same type**.\n\n### 1D Arrays\n\n```java\n// Declaration and Initialization\nint[] numbers = new int[5];        // [0, 0, 0, 0, 0]\nint[] primes = {2, 3, 5, 7, 11};   // Shorthand\n\n// Accessing and Modifying\nprimes[0] = 13;                    // [13, 3, 5, 7, 11]\nSystem.out.println(primes.length); // 5\n```\n\n### 2D Arrays (Matrices)\n\nA 2D array in Java is technically an \"array of arrays\". This structure is fundamental for representing mathematical matrices, grids, and adjacency matrices in graph theory.\n\n```java\n// Create a 3x3 matrix\nint[][] matrix = {\n    {1, 2, 3},\n    {4, 5, 6},\n    {7, 8, 9}\n};\n\n// Access element at row 1, column 2 (value = 6)\nint value = matrix[1][2];\n```\n\n#### Matrix Traversal\nTo process every element in a matrix, use nested loops. The outer loop tracks rows (`matrix.length`), and the inner loop tracks columns (`matrix[row].length`).\n\n```java\nfor (int row = 0; row < matrix.length; row++) {\n    for (int col = 0; col < matrix[row].length; col++) {\n        System.out.print(matrix[row][col] + \" \");\n    }\n    System.out.println(); // Newline after each row\n}\n```\n\n#### Matrix Multiplication\nMultiplying two matrices $A$ (size $m \\times n$) and $B$ (size $n \\times p$) results in a matrix $C$ (size $m \\times p$). This requires three nested loops and runs in $O(m \\cdot n \\cdot p)$ time.\n\n<HighlightCard variant=\"neutral\" title=\"Algorithm: Standard Matrix Multiplication\">\n\n```java\npublic static int[][] multiply(int[][] A, int[][] B) {\n    int rowsA = A.length, colsA = A[0].length;\n    int rowsB = B.length, colsB = B[0].length;\n    \n    // Mathematical prerequisite: A's columns must equal B's rows\n    if (colsA != rowsB) {\n        throw new IllegalArgumentException(\"Invalid dimensions for multiplication\");\n    }\n    \n    int[][] C = new int[rowsA][colsB];\n    \n    // i iterates rows of A, j iterates cols of B, k iterates the shared dimension\n    for (int i = 0; i < rowsA; i++) {\n        for (int j = 0; j < colsB; j++) {\n            int sum = 0;\n            for (int k = 0; k < colsA; k++) {\n                sum += A[i][k] * B[k][j];\n            }\n            C[i][j] = sum;\n        }\n    }\n    return C;\n}\n```\n</HighlightCard>\n\n<Callout type=\"info\" title=\"Jagged Arrays\">\nBecause Java 2D arrays are arrays of references, each row can have a different length. These are called **jagged arrays** or **ragged arrays**. Always use `matrix[row].length` in your inner loop condition rather than assuming a uniform grid.\n</Callout>\n\n---\n\n## 4. Memory Management: Stack vs. Heap\n\n<SafeImage\n  src=\"/images/stack-heap-detailed.png\"\n  alt=\"Stack and Heap Memory\"\n  caption=\"Figure 2: Complete memory model with references and objects\"\n  className=\"w-full max-w-4xl mx-auto\"\n/>\n\n### Stack Memory\n- **Stores**: Primitives + object references\n- **Speed**: Very fast (LIFO)\n- **Lifetime**: Method scope\n- **Size**: Limited (~1MB per thread)\n\n### Heap Memory\n- **Stores**: Actual objects and arrays\n- **Speed**: Slower than stack\n- **Lifetime**: Until no references exist\n- **Management**: Garbage Collector (GC)\n\n```java\npublic void example() {\n    int x = 10;              // Stack: x = 10\n    Car myCar;               // Stack: myCar (null reference)\n    myCar = new Car();       // Heap: Car object created\n                             // Stack: myCar → points to Heap\n}                            // Stack cleared, Heap object\n                             // eligible for GC\n```\n\n> **Garbage Collection**: When no references point to a Heap object, the GC automatically deletes it.\n\n---\n\n## Summary\n\n- **Data Types**: 8 primitives + reference types\n- **Control Flow**: if/else, switch, loops\n- **Arrays**: 1D structures and 2D Matrices (traversal and multiplication)\n- **Memory**: Stack (fast, limited) vs Heap (objects, GC managed)",
  "content/docs/sem3/csi247/01-java-foundations/oop.mdx": "---\ntitle: Object-Oriented Programming (OOP)\ndescription: Classes, objects, the four pillars of OOP, and access control.\n---\n\n# Object-Oriented Programming (OOP)\n\nObject-Oriented Programming (OOP) is a structural paradigm designed to manage software complexity by modeling real-world entities as interacting objects.\n\n---\n\n## 1. User-Defined Classes & Objects\n\nJava is Object-Oriented. **Classes** are blueprints; **Objects** are instances.\n\n### Anatomy of a Class\n\n```java\npublic class Car {\n    // 1. Fields (State)\n    private String model;\n    private int year;\n\n    // 2. Constructor (Initialization)\n    public Car(String model, int year) {\n        this.model = model;  // 'this' = current object\n        this.year = year;\n    }\n\n    // 3. Methods (Behavior)\n    public void drive() {\n        System.out.println(\"The \" + model + \" is driving.\");\n    }\n\n    // 4. Getters/Setters (Encapsulation)\n    public String getModel() { return model; }\n    public void setModel(String model) { this.model = model; }\n}\n```\n\n### Instantiation\n\n```java\nCar myCar = new Car(\"Toyota\", 2022);\nmyCar.drive(); // \"The Toyota is driving.\"\n```\n\n---\n\n## 2. The Four Pillars of OOP: Architectural Mechanics\n\n### 1. Encapsulation: State Protection and Interface Contracts\n\nEncapsulation is the bundling of data (state) and methods (behavior) into a single unit while restricting direct access to internal components. Its primary architectural objective is to enforce class invariants and prevent external state corruption.\n\n#### Access Modifiers and Visibility\nJava enforces encapsulation through a four-tier visibility model.\n\n| Modifier | Class | Package | Subclass | Global | Architectural Use Case |\n| :--- | :---: | :---: | :---: | :---: | :--- |\n| `private` | Yes | No | No | No | Internal state, sensitive data. |\n| *default* | Yes | Yes | No | No | Package-private utilities. |\n| `protected` | Yes | Yes | Yes | No | Methods intended for overriding. |\n| `public` | Yes | Yes | Yes | Yes | The public API contract. |\n\n#### Accessors, Mutators, and Invariant Enforcement\nAccessors (getters) and mutators (setters) are validation gates. By routing all state mutations through mutators, a class can enforce invariants.\n\n```java\npublic class BankAccount {\n    private double balance;\n\n    public void deposit(double amount) {\n        if (amount <= 0) {\n            throw new IllegalArgumentException(\"Deposit must be positive.\");\n        }\n        this.balance += amount;\n    }\n}\n```\n\n<Callout type=\"warn\" title=\"The Hazard of Public State:\">\nIf a class exposes public mutable fields (e.g., `public double balance`), any external module can corrupt the state. Encapsulation forces all state transitions through controlled methods.\n</Callout>\n\n#### Class-Level State: The `static` Keyword\n- **Static Variables**: Shared across all instances in the JVM Method Area.\n- **Static Methods**: Utility functions that operate without instance context (e.g., `Math.max()`). They cannot access instance variables or utilize the `this` reference.\n\n---\n\n### 2. Inheritance: Hierarchical Classification and Coupling\n\nInheritance establishes an \"IS-A\" relationship, allowing a subclass to inherit the state and behavior of a superclass.\n\n#### Constructor Chaining and `super`\nWhen a subclass is instantiated, Java must initialize the superclass state first via `super()`.\n\n```java\npublic class Vehicle {\n    protected String make;\n    public Vehicle(String make) { this.make = make; }\n}\n\npublic class Car extends Vehicle {\n    private int numDoors;\n    public Car(String make, int numDoors) {\n        super(make); // CRITICAL: Must be the first line!\n        this.numDoors = numDoors;\n    }\n}\n```\n\n<Callout type=\"info\" title=\"Architectural Trade-off: Composition over Inheritance\">\nInheritance creates a \"Fragile Base Class\" problem. Use inheritance strictly when a true \"IS-A\" relationship exists. If the goal is merely code reuse, use **Composition** to maintain loose coupling.\n</Callout>\n\n---\n\n### 3. Polymorphism: Dynamic Method Dispatch\n\nPolymorphism allows objects of different concrete classes to be treated as objects of a common superclass. The exact method execution is determined at runtime via **Dynamic Method Dispatch** (vtable).\n\n#### Compile-Time vs. Run-Time Polymorphism\n1.  **Method Overloading (Compile-Time)**: Same name, different parameter signatures.\n2.  **Method Overriding (Run-Time)**: Subclass provides a specific implementation of a superclass method.\n\n```java\n// Upcasting: Treating a specific type as a general type\nList<PaymentMethod> checkoutCart = new ArrayList<>();\ncheckoutCart.add(new CreditCard());\ncheckoutCart.add(new CryptoWallet());\n\n// Dynamic dispatch: The JVM invokes the correct overridden method at runtime\nfor (PaymentMethod pm : checkoutCart) {\n    pm.pay(100.0);\n}\n```\n\n---\n\n### 4. Abstraction: Hiding Implementation Complexity\n\nAbstraction separates the logical properties of an object (the \"what\") from its implementation details (the \"how\").\n\n#### Abstract Classes vs. Interfaces\n\n| Feature | Abstract Class | Interface |\n| :--- | :--- | :--- |\n| **Relationship** | IS-A (Core identity) | CAN-DO / ACTS-AS (Behavioral capability) |\n| **State** | Can maintain instance variables | No instance state (only constants) |\n| **Constructors** | Yes (invoked by subclasses) | No |\n| **Inheritance** | Single inheritance only | Multiple implementation |\n\n#### Design Principle: Program to an Interface\n```java\n// Rigid: Tied to a specific implementation\nArrayList<String> list = new ArrayList<>();\n\n// Flexible: Tied to an abstraction\nList<String> list = new ArrayList<>();\n```\n\n---\n\n## 3. Packages & Access Control\n\n### Packages\nOrganize classes into namespaces (like folders).\n\n```java\npackage com.university.csi247.models;\nimport java.util.ArrayList;\n```\n\n<SafeImage\n  src=\"/images/access-modifiers-chart.png\"\n  alt=\"Access Modifiers Visibility\"\n  caption=\"Figure 1: Visibility scope of each modifier\"\n  className=\"w-full max-w-4xl mx-auto\"\n/>\n\n---\n\n## Summary\n\n- **Classes/Objects**: Blueprints and instances.\n- **Encapsulation**: State protection via access modifiers.\n- **Inheritance**: Hierarchical classification via `extends`.\n- **Polymorphism**: Dynamic method dispatch via overriding.\n- **Abstraction**: Hiding complexity via interfaces and abstract classes.",
  "content/docs/sem3/csi247/01-java-foundations/generics.mdx": "---\ntitle: Java Generics & Type Erasure\ndescription: Type-safe collections, bounded wildcards, PECS, and the mechanics of type erasure.\n---\n\n# Java Generics & Type Erasure\n\nGenerics provide compile-time type safety, eliminating the need for explicit casting and preventing `ClassCastException` at runtime.\n\n---\n\n## 1. Why Generics?\n\n**Without Generics** (Unsafe):\n```java\nList list = new ArrayList();\nlist.add(\"Hello\");\nString s = (String) list.get(0); // Risky cast!\n```\n\n**With Generics** (Safe):\n```java\nList<String> list = new ArrayList<>();\nlist.add(\"Hello\");\nString s = list.get(0); // No cast needed!\n```\n\n<SafeImage\n  src=\"/images/generics-safety.png\"\n  alt=\"Generics Type Safety\"\n  caption=\"Figure 1: Compile-time checking prevents runtime errors\"\n  className=\"w-full max-w-3xl mx-auto\"\n/>\n\n---\n\n## 2. Generic Classes & Methods\n\n```java\npublic class Box<T> {\n    private T item;\n\n    public void setItem(T item) {\n        this.item = item;\n    }\n\n    public T getItem() {\n        return item;\n    }\n}\n\nBox<Integer> intBox = new Box<>();\nintBox.setItem(10);\n```\n\n---\n\n## 3. Bounded Type Parameters\n\nYou can restrict the types that can be passed as generic parameters.\n\n```java\n// T must extend Number (or be Number itself)\npublic <T extends Number> void printValue(T num) {\n    System.out.println(num.doubleValue());\n}\n```\n\n---\n\n## 4. Wildcards & PECS (Producer Extends, Consumer Super)\n\nWildcards (`?`) allow methods to accept a broader range of generic types.\n\n1. **`? extends T`** (Upper Bound): Read-only. The collection is a **Producer**.\n2. **`? super T`** (Lower Bound): Write-only. The collection is a **Consumer**.\n3. **`?`** (Unbounded): Any type.\n\n<SafeImage\n  src=\"/images/wildcard-types.png\"\n  alt=\"Generic Wildcards\"\n  caption=\"Figure 2: PECS rule - Producer Extends, Consumer Super\"\n  className=\"w-full max-w-3xl mx-auto\"\n/>\n\n<HighlightCard variant=\"neutral\" title=\"The PECS Principle in Action\">\n\n```java\n// We are reading from 'src' (Producer) and writing to 'dest' (Consumer)\npublic static <T> void copy(List<? super T> dest, List<? extends T> src) {\n    for (T item : src) {\n        dest.add(item);\n    }\n}\n```\nIf we used `List<? extends T>` for `dest`, the compiler would block the `add()` operation because it cannot guarantee the exact subtype of the list.\n</HighlightCard>\n\n---\n\n## 5. Type Erasure & The Array Restriction\n\nTo maintain backward compatibility with pre-Java 5 code, the JVM implements generics via **type erasure**. The compiler removes all type parameters, replacing them with their bounds or `Object`.\n\n<Callout type=\"warn\" title=\"The Generic Array Restriction\">\nYou **cannot** instantiate a generic array in Java (e.g., `new T[10]` is illegal). \n\n**Why?** Arrays in Java are **reified** (they know their component type at runtime and enforce it via `ArrayStoreException`). Generics are **erased** (they do not exist at runtime). If `new T[]` were allowed, the JVM would not know the actual runtime type to enforce array safety.\n\n**The Standard Workaround:**\nWhen implementing data structures like `ArrayList` or `HashMap` from scratch, you must create an `Object` array and cast it, suppressing the unchecked warning.\n\n```java\n@SuppressWarnings(\"unchecked\")\nT[] elements = (T[]) new Object[capacity];\n```\n</Callout>\n\n---\n\n## Summary\n\n- **Generics**: Enforce type safety at compile time.\n- **Bounded Types**: Restrict generics to specific hierarchies (`<T extends Number>`).\n- **PECS**: Use `? extends T` for reading (Producers) and `? super T` for writing (Consumers).\n- **Type Erasure**: Generics are a compile-time illusion; at runtime, they revert to `Object` or their bound.",
  "content/docs/sem3/csi247/01-java-foundations/file-io.mdx": "---\ntitle: File I/O & String Parsing\ndescription: Reading files, parsing strings, and handling malformed data using Java's I/O streams.\n---\n\n# File I/O & String Parsing\n\nProcessing external data is a fundamental skill. This guide covers reading files efficiently, tokenizing strings, and building robust parsers that survive malformed input.\n\n---\n\n## 1. The File I/O Ecosystem\n\nJava provides two primary ways to read text files. Choosing the right one impacts performance and memory usage.\n\n### Scanner vs. BufferedReader\n\n| Feature | `Scanner` | `BufferedReader` |\n| :--- | :--- | :--- |\n| **Speed** | Slow (parses tokens using regex) | Fast (reads raw character buffers) |\n| **Memory** | High overhead | Low overhead |\n| **Use Case** | Simple console input, small files | Large files, log parsing, competitive programming |\n| **Thread Safety** | Not thread-safe | Not thread-safe |\n\n<HighlightCard variant=\"neutral\" title=\"Best Practice: BufferedReader + InputStreamReader\">\n\n```java\nimport java.io.BufferedReader;\nimport java.io.FileReader;\nimport java.io.IOException;\n\npublic class FileParser {\n    public void readLargeFile(String filePath) throws IOException {\n        // try-with-resources automatically closes the stream\n        try (BufferedReader br = new BufferedReader(new FileReader(filePath))) {\n            String line;\n            while ((line = br.readLine()) != null) {\n                // Process line\n                System.out.println(line);\n            }\n        }\n    }\n}\n```\n</HighlightCard>\n\n---\n\n## 2. String Parsing & Tokenization\n\nOnce you have a line of text, you need to extract specific data fields.\n\n### `String.split()`\nThe most common way to break a string into an array of substrings based on a regular expression delimiter.\n\n```java\nString csvLine = \"Alice,25,Engineer\";\nString[] parts = csvLine.split(\",\");\n\nString name = parts[0];       // \"Alice\"\nint age = Integer.parseInt(parts[1]); // 25\nString role = parts[2];       // \"Engineer\"\n```\n\n<Callout type=\"warn\" title=\"Regex Pitfalls with split()\">\nThe argument to `split()` is a **Regular Expression**. If your delimiter is a special regex character (like `|`, `.`, `*`, `+`), you must escape it!\n\n- **Wrong**: `line.split(\"|\")` (Crashes or behaves unexpectedly)\n- **Right**: `line.split(\"\\\\|\")` or `line.split(Pattern.quote(\"|\"))`\n</Callout>\n\n---\n\n## 3. Resource Management: `try-with-resources`\n\nBefore Java 7, developers had to write verbose `finally` blocks to ensure file streams were closed, preventing memory leaks and file locks.\n\n```java\n// The Modern Way (Java 7+)\ntry (BufferedReader br = new BufferedReader(new FileReader(\"data.txt\"))) {\n    // Use br\n} catch (IOException e) {\n    e.printStackTrace();\n}\n// br is automatically closed here, even if an exception is thrown!\n```\n\n---\n\n## 4. Handling Malformed Data (Robust Parsing)\n\nIn the real world (and in exams), input files are rarely perfect. Lines might be missing columns, or numbers might contain letters. A robust parser must skip bad lines without crashing the entire program.\n\n<StepByStep>\n<StepByStepTitle>Exam Scenario: Parsing Server Logs</StepByStepTitle>\n<Step number={1}>\n<StepTitle>The Problem</StepTitle>\nRead a log file line-by-line. Each line is formatted as `TIMESTAMP|ACTION|USER`. \n- `LOGIN` adds 1 to concurrent users.\n- `LOGOUT` subtracts 1.\n- Calculate the peak concurrent users.\n</Step>\n<Step number={2}>\n<StepTitle>The Robust Implementation</StepTitle>\n\n```java\npublic int findPeakConcurrentUsers(String filePath) throws IOException {\n    int currentUsers = 0;\n    int maxUsers = 0;\n\n    try (BufferedReader br = new BufferedReader(new FileReader(filePath))) {\n        String line;\n        while ((line = br.readLine()) != null) {\n            try {\n                // Split by the pipe character (escaped for regex)\n                String[] parts = line.split(\"\\\\|\");\n                \n                // Validate column count\n                if (parts.length < 2) continue; \n                \n                String action = parts[1].trim();\n                \n                if (\"LOGIN\".equals(action)) {\n                    currentUsers++;\n                    maxUsers = Math.max(maxUsers, currentUsers);\n                } else if (\"LOGOUT\".equals(action)) {\n                    currentUsers--;\n                }\n            } catch (Exception e) {\n                // Catch ArrayIndexOutOfBoundsException, NullPointerException, etc.\n                // Skip malformed lines and continue to the next\n                System.err.println(\"Skipping malformed line: \" + line);\n                continue;\n            }\n        }\n    }\n    return maxUsers;\n}\n```\n</Step>\n<Step number={3}>\n<StepTitle>Why this works</StepTitle>\nBy wrapping the parsing logic of a *single line* inside a `try-catch` block *inside* the `while` loop, an exception on line 42 won't crash the program. The `catch` block logs the error, and the `continue` statement safely advances the reader to line 43.\n</Step>\n</StepByStep>\n\n---\n\n## Summary\n\n- **BufferedReader**: Use for fast, line-by-line file reading.\n- **try-with-resources**: Mandatory for preventing file handle leaks.\n- **String.split()**: Remember that the delimiter is a Regex; escape special characters like `|` or `.`.\n- **Error Handling**: Wrap individual line parsing in `try-catch` blocks to build resilient data pipelines.",
  "content/docs/sem3/csi247/01-java-foundations/meta.json": "{\n  \"title\": \"1. Java Foundations\",\n  \"pages\": [\n    \"basic\",\n    \"oop\",\n    \"generics\",\n    \"file-io\",\n    \"java-collections\",\n    \"questions\",\n    \"review\",\n    \"resources\"\n  ]\n}"
}
