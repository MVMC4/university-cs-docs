---
title: Chapter Review
description: Self-assessment checklist, flashcards, and quizzes for Graphs.
---

# Chapter Review

import { flashcardData } from '@/content/flashcard-data/sem3/csi247/graphs/flashcards';
import { quizData as graphsQuiz } from '@/content/quiz-data/sem3/csi247/graphs/graphs-quiz';
import { quizData as traversalsQuiz } from '@/content/quiz-data/sem3/csi247/graphs/representations-traversals-quiz';

## Self-Assessment Checklist

<Checklist
  title="Chapter 8  --  You Should Be Able To..."
  rules={[
    { type: "must", text: "Convert between adjacency matrix and adjacency list representations for a directed weighted graph." },
    { type: "must", text: "Trace BFS on a graph by hand using a queue, showing the visited array state after each dequeue." },
    { type: "must", text: "Trace DFS on a graph by hand (both recursive and iterative-with-stack versions)." },
    { type: "must", text: "Run Dijkstra's algorithm by hand on a weighted graph, maintaining the distance table and priority queue." },
    { type: "must", text: "Explain why Dijkstra's fails on negative-weight edges and how Bellman-Ford handles them." },
    { type: "must", text: "State the condition for Topological Sort (DAG only) and produce one valid ordering." },
    { type: "avoid", text: "Marking a node as visited after dequeuing in BFS  --  it must be marked when enqueued to prevent duplicate entries." },
    { type: "avoid", text: "Using BFS for unweighted shortest path but Dijkstra's where BFS would suffice  --  Dijkstra adds unnecessary overhead on unweighted graphs." },
    { type: "note", text: "Union-Find with path compression + union by rank achieves near-O(1) amortized operations per query." },
  ]}
/>

---

## Flashcards

<FlashcardDeck cards={flashcardData} title="Graphs Deck" />

---

## Quick Quiz

<Quiz
  title="Chapter 8 Quick Check"
  questions={[
    {
      question: "When should you use BFS over DFS for finding the shortest path between two nodes?",
      options: [
        "Always — BFS is always faster",
        "When the graph is weighted and all weights are positive",
        "When the graph is unweighted (or all weights equal) and you want the fewest edges",
        "When the graph is a DAG"
      ],
      correctIndex: 2,
      explanation: "BFS explores level by level, guaranteeing the minimum number of edges to reach a node in an unweighted graph. For weighted graphs, use Dijkstra's."
    }
  ]}
/>

---

## Full Chapter Assessments

<Quiz title="Graphs Assessment" questions={graphsQuiz} />
<Quiz title="Representations & Traversals Deep Dive" questions={traversalsQuiz} />
