export const quizData = [{"question":"Why is the `Node` class in a `LinkedList` typically declared as `static`?","options":["To allow it to access the outer class's private methods.","To prevent it from holding an implicit reference to the outer `LinkedList` instance, saving memory.","To make it serializable.","To allow it to be instantiated without an outer class instance."],"correctIndex":1,"explanation":"A non-static inner class holds a hidden `this$0` reference to the outer class. For millions of nodes, this wastes significant memory and can cause memory leaks."},{"question":"Why is the `Node` class declared as `private`?","options":["To prevent memory leaks.","To enforce encapsulation, hiding the internal structure from the client.","To make it run faster.","Because Java requires inner classes to be private."],"correctIndex":1,"explanation":"Encapsulation dictates that the internal implementation details (like `Node`) should not be exposed to the user of the `LinkedList` API."},{"question":"In the method `addAll(Collection<? extends T> c)`, what does `? extends T` signify?","options":["The collection is a Consumer of elements.","The collection is a Producer of elements, and we can safely read items of type `T` or its subtypes from it.","The collection must be exactly of type `T`.","It prevents null values."],"correctIndex":1,"explanation":"PECS: Producer Extends. We are reading from `c`, so it can be a collection of `T` or any subtype of `T`."},{"question":"What is the purpose of the `modCount` field in a `LinkedList`?","options":["To count the number of nodes.","To track the number of structural modifications for the fail-fast iterator.","To limit the maximum size of the list.","To calculate the hash code of the list."],"correctIndex":1,"explanation":"`modCount` is incremented on every structural change. Iterators check it to detect concurrent modifications."},{"question":"What exception is thrown by a fail-fast iterator if the list is modified during iteration?","options":["`NullPointerException`","`IllegalStateException`","`ConcurrentModificationException`","`IndexOutOfBoundsException`"],"correctIndex":2,"explanation":"The iterator detects that `modCount != expectedModCount` and throws `ConcurrentModificationException` to prevent undefined behavior."},{"question":"Which interface must a `LinkedList` implement to be used in an enhanced `for-each` loop?","options":["`Iterator<T>`","`Collection<T>`","`Iterable<T>`","`Comparable<T>`"],"correctIndex":2,"explanation":"The enhanced for-each loop requires the target to implement `Iterable<T>`, which provides the `iterator()` method."},{"question":"Why can't we use `instanceof Node<T>` in Java?","options":["Because `Node` is private.","Because of type erasure; generic type information is not available at runtime.","Because `Node` is static.","Because `instanceof` only works for interfaces."],"correctIndex":1,"explanation":"Type erasure removes generic type parameters at runtime. You can only check `instanceof Node<?>` or raw `Node`."},{"question":"What is the time complexity of adding an element to the end of a singly linked list WITHOUT a tail pointer?","options":["$O(1)$","$O(\\log n)$","$O(n)$","$O(n^2)$"],"correctIndex":2,"explanation":"Without a tail pointer, the list must be traversed from the head to the last node, taking linear time."},{"question":"In the `LinkedListIterator`, when is `expectedModCount` initialized?","options":["When the list is created.","When the iterator is created.","When `next()` is called for the first time.","When the list is fully traversed."],"correctIndex":1,"explanation":"The iterator captures the list's `modCount` at the moment of its creation to establish the baseline for detecting future modifications."},{"question":"If `LinkedList` implements `Iterable<T>`, what method must it provide?","options":["`hasNext()`","`next()`","`iterator()`","`add(T item)`"],"correctIndex":2,"explanation":"`Iterable<T>` requires the `iterator()` method, which returns an `Iterator<T>` instance."},{"question":"What is the memory overhead of a non-static inner class instance on a 64-bit JVM?","options":["0 bytes","4 bytes","8 bytes (for the hidden `this$0` reference)","16 bytes"],"correctIndex":2,"explanation":"A non-static inner class holds an implicit reference to the outer instance, which costs 8 bytes on a 64-bit JVM."},{"question":"Why do we use `Node<T>` instead of `Node` (raw type) in the `LinkedList` implementation?","options":["To make it run faster.","To maintain compile-time type safety and avoid explicit casting.","Because Java requires it for inner classes.","To reduce memory usage."],"correctIndex":1,"explanation":"Generics provide compile-time type checking, ensuring that only objects of type `T` are stored in the list, eliminating the need for casting when retrieving elements."},{"question":"In the `addAll(Collection<? extends T> c)` method, what would happen if we used `Collection<T>` instead?","options":["It would run faster.","We wouldn't be able to pass a `Collection<SubtypeOfT>` to the method.","It would cause a compilation error.","It would allow adding nulls."],"correctIndex":1,"explanation":"Using `Collection<T>` makes the method invariant. `? extends T` allows covariance, letting us pass collections of subtypes."},{"question":"What happens if an iterator's `remove()` method is called twice without calling `next()` in between?","options":["It removes the next element.","It throws an `IllegalStateException`.","It does nothing.","It throws `ConcurrentModificationException`."],"correctIndex":1,"explanation":"The `Iterator` contract specifies that `remove()` can only be called once per call to `next()`. Calling it again throws `IllegalStateException`."},{"question":"Why is the `Node` class's constructor typically `private` or package-private?","options":["To prevent the client from creating nodes directly and bypassing the list's logic.","To save memory.","Because Java requires it.","To make it thread-safe."],"correctIndex":0,"explanation":"Encapsulation ensures that nodes are only created and linked by the `LinkedList`'s own methods, maintaining the structural integrity of the list."}];